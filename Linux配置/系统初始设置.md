# 系统初始设置

```
sudo apt -y install git gnome-tweaks chrome-gnome-shell gnome-tweak-tool stacer peek flameshot screenkey mpv vim git gparted guake gnome-boxes lollypop calibre krita gimp kdenlive nomacs rhythmbox folder-color gnome-sushi nautilus-admin 
```
```
wget -q- https://raw.githubusercontent.com/cra0zy/code-nautilus/master/install.sh | bash
```
```
sudo chown -R $(whoami) /usr/share/code
```

